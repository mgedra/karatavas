 encoding='utf-8'
import pygame
import os
        Lai varētu lietot arī tās funkcijas, kas nav automātiski pieejamas
pygame.init()
        Lai varētu sākt lietot.
    WIDTH, HEIGHT = 800, 500
    pygame.display.set_mode((WIDTH, HEIGHT))
        Definēti kā lielie burti, jo nemainās.
    pygame.display.set_caption("Karātavas!")

    FSP = 60
        Lai noteiktu "spēles ātrumu"
    clock = pygame.time.Clock()

    while run:
        clock.tick(FPS)

        for event in pygame.event.get():
            if event.type == pygame.QUIT:
                run = False
pygame.quit()
